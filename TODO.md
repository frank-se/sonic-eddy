# TODO

- Swap position of sends and filter sections in channel UI to better reflect the
  actual signal path
- Remove Audio To section from group channels
- Midi Router should show routes that are not active but stored, maybe grayed
  out or so. Currently it's confusing when a link suddenly shows up because both
  devices showed up
