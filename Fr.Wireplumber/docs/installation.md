# Installation

FrWireplumber depends on `pipewire` and `wireplumber`, so please install with the
package manager of you Linux distribution. Afterwards, you can add the package with
`dotnet add package FrWireplumber`.
